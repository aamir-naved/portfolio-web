# Optional: in-browser contact form (no backend). https://web3forms.com
# Copy this file to .env.local and set the key there for local dev.
# For production, add NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY in your hosting env (e.g. Vercel).
# Do not put a real key in this file if you commit .env.example to git.
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
